{
  "name": "fitbit-metrics",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "deploy": "wrangler deploy",
    "dev": "wrangler dev",
    "start": "wrangler dev",
    "test": "vitest",
    "cf-typegen": "wrangler types",
    "fmt": "prettier --write .",
    "pre-commit": "prettier --write . && wrangler types"
  },
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "^0.13.0",
    "@types/node": "^25.5.0",
    "prettier": "^3.8.1",
    "typescript": "^5.5.2",
    "vitest": "^4.1.0",
    "wrangler": "^4.73.0"
  },
  "dependencies": {
    "@hono/zod-openapi": "^1.2.2",
    "@scalar/hono-api-reference": "^0.10.3",
    "hono": "^4.12.8",
    "marked": "^17.0.4"
  }
}
